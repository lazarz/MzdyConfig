server:
  port: 8102
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
spring:
  messages:
    basename: messages
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
